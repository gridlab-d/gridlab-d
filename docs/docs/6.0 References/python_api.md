# Base Python API

These functions are the base-level Python API calls implemented in GridLAB-D™. We expect most users will use the Python class for interacting with GridLAB-D; that class is effectively a wrapper on many of these APIs. That said, these APIs are publicly exposed and available to all users.


:::gridlabd._isolated